# Switchyard — minimal runtime + dev dependencies.
# scipy is pulled in transitively by scikit-learn.
numpy>=1.26
scikit-learn>=1.3
matplotlib>=3.7
jsonschema>=4.0
pytest>=7.0

# LLM diagnosis (§8 / TASK 2). Gemini is primary; OpenAI is a fallback; Anthropic
# is selectable. python-dotenv loads .env. Keys are read from the environment.
google-genai>=1.0
openai>=1.40
python-dotenv>=1.0
anthropic>=0.40
